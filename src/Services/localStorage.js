// This will get the provided key from our local storage
export function getLocalStorageValue(key) {
  const localStorage = window.localStorage;
  let companyReference = localStorage.getItem("reference");
  //console.log(companyReference);
}
